def inline convert(x: Int) -> Int {
    return 0;
}

def inline [ + ](x: Int, y: Int) -> Int {
    return %iadd(x, y);
}

def inline [ - ](x: Int, y: Int) -> Int {
    return %isub(x, y);
}

def inline [ * ](x: Int, y: Int) -> Int {
    return %imul(x, y);
}

def inline [ / ](x: Int, y: Int) -> Int {
    return %idiv(x, y);
}

def inline [ % ](x: Int, y: Int) -> Int {
    return %imod(x, y);
}

def main() -> Int {
    return 16 / 2 + 3;
}
