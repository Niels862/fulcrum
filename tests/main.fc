def f(x: Int, y: Int) -> Int {
    return x;
}

def main() -> Int {
    return %iadd(g(), %isub(%imul(2, 14), 1)); 
}

def g() -> Int {
    return f(42, 12);
}

def g(x: Int) -> Int {
    return 53;
}
